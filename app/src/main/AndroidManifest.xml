<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wyang.study">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".global.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ContainerActivity" />
        <activity android:name=".ui.activity.AActivity" />
        <activity
            android:name=".ui.activity.BActivity"
            android:taskAffinity="com.wyang.study.ui.activity.task"
            android:launchMode="singleTask" />
        <activity android:name=".ui.activity.CActivity" />

        <service
            android:name=".global.InitIntentService"
            android:exported="false" />

        <service
            android:name=".ui.DownLoadIntentService"
            android:exported="false" />

        <provider
            android:name=".ui.TestContentProvider"
            android:authorities="com.wyang.study.provider"
            android:multiprocess="true"
            android:process=":cooooddddd" />
    </application>

</manifest>